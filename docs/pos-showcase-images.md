# POS: interfaz de demostración

Capturas adaptadas con imagegen integrado para anonimizar datos.

## registers

Asset: `imgs/pos-registers-demo-v1.png`

Edit ONLY image 1 of the latest user-provided three POS screenshots (Cajas y turnos table). Preserve full original aspect ratio, original layout, controls, Spanish UI labels, coral accents and exact functionality. Privacy sanitation only: replace personal names with Colaborador demo, company/business/location names with Negocio demo and Sucursal demo, warehouse names with Almacén demo, cash register identifiers with Caja 01. Replace product names/descriptions/SKUs with Producto 01 and SKU-001, replace property photos with neutral product thumbnails. For restaurant table-map use Mesa 01, Mesa 02 etc instead of all personal table names. Replace financial amounts with $ —, preserve status labels and table seats. Replace avatar photo with neutral gray circle. Remove floating assistant mascot if present. Return only this single sanitized screenshot without additional marketing frames.

## retail

Asset: `imgs/pos-retail-demo-v1.png`

Edit ONLY image 2 of the latest user-provided three POS screenshots (wide Point of Sale product catalog and current ticket). Preserve full original aspect ratio, original layout, controls, Spanish UI labels, coral accents and exact functionality. Privacy sanitation only: replace personal names with Colaborador demo, company/business/location names with Negocio demo and Sucursal demo, warehouse names with Almacén demo, cash register identifiers with Caja 01. Replace product names/descriptions/SKUs with Producto 01 and SKU-001, replace property photos with neutral product thumbnails. For restaurant table-map use Mesa 01, Mesa 02 etc instead of all personal table names. Replace financial amounts with $ —, preserve status labels and table seats. Replace avatar photo with neutral gray circle. Remove floating assistant mascot if present. Return only this single sanitized screenshot without additional marketing frames.

## restaurant

Asset: `imgs/pos-restaurant-demo-v1.png`

Edit ONLY image 3 of the latest user-provided three POS screenshots (tall restaurant table-map kiosk). Preserve full original aspect ratio, original layout, controls, Spanish UI labels, coral accents and exact functionality. Privacy sanitation only: replace personal names with Colaborador demo, company/business/location names with Negocio demo and Sucursal demo, warehouse names with Almacén demo, cash register identifiers with Caja 01. Replace product names/descriptions/SKUs with Producto 01 and SKU-001, replace property photos with neutral product thumbnails. For restaurant table-map use Mesa 01, Mesa 02 etc instead of all personal table names. Replace financial amounts with $ —, preserve status labels and table seats. Replace avatar photo with neutral gray circle. Remove floating assistant mascot if present. Return only this single sanitized screenshot without additional marketing frames.