# Ventas: capturas adaptadas

Anonimizadas mediante imagegen integrado.

## pipeline

`imgs/sales-pipeline-demo-v1.png`

Edit only screenshot 1 from the user's latest three Sales screenshots (pipeline). Preserve full original layout, aspect ratio, Spanish and English interface labels, coral accents, blue header, columns, controls and statuses. Privacy edits only: replace all personal names (including truncated ones), company names, client names and employee names with Cliente demo, Empresa demo, Colaborador demo as appropriate; greeting Colaborador demo; neutral gray avatar. Replace product and property identifiers with Producto 01 and generic sale/reference IDs. Replace financial amounts throughout with em dash retaining currency labels. Keep text legible, no redesign or new features, no marketing frame. Return one sanitized screenshot.

## quotes

`imgs/sales-quotes-demo-v1.png`

Edit only screenshot 2 from the user's latest three Sales screenshots (quotes). Preserve full original layout, aspect ratio, Spanish and English interface labels, coral accents, blue header, columns, controls and statuses. Privacy edits only: replace all personal names (including truncated ones), company names, client names and employee names with Cliente demo, Empresa demo, Colaborador demo as appropriate; greeting Colaborador demo; neutral gray avatar. Replace product and property identifiers with Producto 01 and generic sale/reference IDs. Replace financial amounts throughout with em dash retaining currency labels. Keep text legible, no redesign or new features, no marketing frame. Return one sanitized screenshot.

## commissions

`imgs/sales-commissions-demo-v1.png`

Edit only screenshot 3 from the user's latest three Sales screenshots (commissions). Preserve full original layout, aspect ratio, Spanish and English interface labels, coral accents, blue header, columns, controls and statuses. Privacy edits only: replace all personal names (including truncated ones), company names, client names and employee names with Cliente demo, Empresa demo, Colaborador demo as appropriate; greeting Colaborador demo; neutral gray avatar. Replace product and property identifiers with Producto 01 and generic sale/reference IDs. Replace financial amounts throughout with em dash retaining currency labels. Keep text legible, no redesign or new features, no marketing frame. Return one sanitized screenshot.